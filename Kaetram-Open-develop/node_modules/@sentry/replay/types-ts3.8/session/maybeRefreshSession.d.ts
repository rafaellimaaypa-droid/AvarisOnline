import { Session, SessionOptions } from '../types';
/**
 * Check a session, and either return it or a refreshed version of it.
 * The refreshed version may be unsampled.
 * You can check if the session has changed by comparing the session IDs.
 */
export declare function maybeRefreshSession(session: Session, { traceInternals, maxReplayDuration, sessionIdleExpire, }: {
    sessionIdleExpire: number;
    maxReplayDuration: number;
    traceInternals?: boolean;
}, sessionOptions: SessionOptions): Session;
//# sourceMappingURL=maybeRefreshSession.d.ts.map
